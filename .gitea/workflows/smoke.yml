name: orbit-smoke

on:
  workflow_dispatch:
  push:

jobs:
  rust-trixie-arm64:
    runs-on: rust-ci-trixie
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - name: Native Linux environment
        run: uname -a
      - name: Orbit smoke test
        run: just smoke
