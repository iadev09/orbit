[package]
name = "orbit-metrics"
version = "0.2.1"
edition.workspace = true
authors.workspace = true
description = "Metrics snapshot families over orbit-rs rings."
license.workspace = true
repository.workspace = true
documentation = "https://docs.rs/orbit-metrics"
keywords = ["metrics", "shm", "fleet", "orbit","ring"]

[dependencies]
bytes.workspace = true
orbit-rs = { version = "0.2.1", path = "../orbit-rs" }
