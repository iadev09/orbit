[package]
name = "orbit-metrics"
version = "0.1.0"
edition.workspace = true
authors.workspace = true
description = "Metrics snapshot families over orbit-rs rings."
license.workspace = true
repository.workspace = true
readme = "README.md"
documentation = "https://docs.rs/orbit-metrics"

[dependencies]
bytes.workspace = true
orbit-rs = { version = "0.1.0", path = "../orbit-rs" }
