[package]
name = "orbit-lock"
version = "0.2.1"
edition.workspace = true
authors.workspace = true
description = "Fleet-shared keyed locks over Orbit shared memory."
license.workspace = true
repository.workspace = true
keywords = ["lock", "shm", "fleet", "orbit","ring"]
categories = ["concurrency"]

[dependencies]
bytes.workspace = true
orbit-rs = { path = "../orbit-rs", version = "0.2.1" }

[target.'cfg(unix)'.dependencies]
libc.workspace = true

[dev-dependencies]
nix.workspace = true

[target.'cfg(unix)'.dev-dependencies]
libc.workspace = true
