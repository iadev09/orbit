[package]
name = "orbit-event"
version = "0.2.1"
edition.workspace = true
authors.workspace = true
description = "Fleet-shared topic event streams over Orbit rings."
license.workspace = true
repository.workspace = true
keywords = ["event", "shm", "fleet", "orbit","ring"]
categories = ["concurrency", "data-structures"]
documentation = "https://docs.rs/orbit-event"

[dependencies]
bytes.workspace = true
orbit-rs = { path = "../orbit-rs", version = "0.2.1" }

[target.'cfg(unix)'.dev-dependencies]
libc.workspace = true
nix.workspace = true
